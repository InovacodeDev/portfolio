{
    "version": 2,
    "builds": [
        {
            "src": "apps/web/package.json",
            "use": "@vercel/next",
            "config": {
                "buildCommand": "cd ../.. && pnpm turbo run build --filter=@inovacode/web"
            }
        }
    ],
    "installCommand": "pnpm install"
}