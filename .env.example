# Environment Variables for Inovacode Portfolio

# Database Configuration (Supabase PostgreSQL)
# Get these values from your Supabase Dashboard > Settings > Database

# Direct Connection (for persistent servers, requires IPv6)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Session Mode (for persistent servers with IPv4 support)
DATABASE_SESSION_URL="postgres://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# Transaction Mode (for serverless functions)
DATABASE_TRANSACTION_URL="postgres://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres"

# Supabase Project Configuration
SUPABASE_URL="https://[PROJECT-REF].supabase.co"
SUPABASE_ANON_KEY="[YOUR-ANON-KEY]"
SUPABASE_SERVICE_ROLE_KEY="[YOUR-SERVICE-ROLE-KEY]"

# Database Password (used in connection strings above)
DB_PASSWORD="[YOUR-DATABASE-PASSWORD]"

# API Configuration
API_PORT=3000
API_HOST="0.0.0.0"

# Application Environment
NODE_ENV="development"

# Security
JWT_SECRET="[GENERATE-A-STRONG-JWT-SECRET]"

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all [PLACEHOLDER] values with actual values from your Supabase project
# 3. Generate a strong JWT secret for the JWT_SECRET field
# 4. Never commit .env.local to version control