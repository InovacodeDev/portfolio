# Environment Variables for Inovacode Portfolio

# Database Configuration (Supabase PostgreSQL)
# Get these values from your Supabase Dashboard > Settings > Database

# Direct Connection (for persistent servers, requires IPv6)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# API Configuration
API_PORT="3000"
API_HOST="0.0.0.0"

# Application Environment
NODE_ENV="development"
# For local development the API URL points to local API instance
VITE_API_URL="http://localhost:3001"

# In production, when frontend and API are hosted on the same origin (for
# example a Vercel static site with serverless functions), set VITE_API_URL to
# an empty string to use same-origin relative paths (recommended):
# VITE_API_URL=""

# Or explicitly point to your API domain:
# VITE_API_URL="https://api.yoursite.com"

# Resend (email service) - required for contact notifications
# Get your API key from https://resend.com
RESEND_API_KEY="re_your_resend_api_key"

# Email sending configuration
# Comma-separated list of admin recipients for contact notifications
EMAIL_TO="admin@example.com"
# Optional from address (defaults to first EMAIL_TO)
EMAIL_FROM="no-reply@example.com"

# Optional site branding used by email templates
SITE_LOGO_URL="https://your-site.com/path/to/logo.svg"
SITE_URL="https://inovacode.dev"

REDIS_URL="redis://:password@redis-host:6379"

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all [PLACEHOLDER] values with actual values from your Supabase project
# 3. Generate a strong JWT secret for the JWT_SECRET field
# 4. Never commit .env.local to version control